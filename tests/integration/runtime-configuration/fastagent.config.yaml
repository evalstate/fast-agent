execution_engine: asyncio
default_model: gpt-4.1

logger:
  type: file
  level: error
  truncate_tools: true

mcp:
  servers:
    env_get_server:
      command: "uv"
      args: ["run", "mcp_server.py"]
    env:
      TOKEN: "DEFAULT_TOKEN"
